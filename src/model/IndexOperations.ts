
enum INDEX_OPERATIONS {
    DATASETS = "datasets",
    MODELS = "models",
    PROJECTS = "projects",
    LICENSES = "licenses",
    USERS = "users",
    SITES = "sites",
    DATASET_ACCESS = "datasetAccess"
}

export default INDEX_OPERATIONS;